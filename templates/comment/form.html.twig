   
        <h3>Add comment</h3>
        <div {#class="card"#}>
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                {{ form_start(form) }}
                <div class="form-group">
                    {#<label for="textarea">{{ form_label(form.content) }}</label>#}
                    {{ form_widget(form.content) }}
                </div>
                <button type="submit" class="btn btn-success">send</button>
                {{ form_end(form) }}
            </div>
        </div>
        <br>

   
